version: 1
frontend:
  phases:
    build:
      commands:
        - npm ci
        - npm run build --if-present
  artifacts:
    baseDirectory: dist/quien-presta-web/browser
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
